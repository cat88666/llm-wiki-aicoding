# 预站起 / 留座离桌 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：1045 `UserPrepStandCommand`（PLAYER）、1034 `UserHoldSeatCommand`（PLAYER，新增）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；P1=1045 由死功能激活为可用，需客户端确认；协议字段 0 改动）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-032 ~ 035（R-01 与 20 S-02 混在 `036cfd439`）
- 范围内文件：`UserStandPrepBootstrapServiceImpl`(A)、`UserHoldSeatCommand`(A)、`UserPrepStandCommandDefault`(M)、`UserPrepStandReqDto`(M 注释)、`PrepStandStatusEnum`(M 注释)、`UserStandPrepBootstrapService`(M Javadoc)
- 相邻（属未设计 visitor→Provider 重构，见 §8）：`PrepStandLeaveUserVisitor`→`PrepStandLeaveCandidateProvider`、`PrepKickLeaveUserVisitor`→`PrepKickLeaveCandidateProvider`

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1045 | 请求 | `UserPrepStandReqDto` | 无（仅 `standType` 注释修正，字段定义不变） | 完全兼容 |
| 1045 | 响应 | `UserPrepStandRespDto` | 无（`after` 补 `setUserId` 是赋值，非字段定义变更） | 完全兼容 |
| 1034 | 请求/响应 | `BaseNetRequestDto` + 通用错误响应 | 无新 DTO | 完全兼容 |

**协议字段 0 改动。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 1045 可用性 | **无 `UserStandPrepBootstrapService` 实现** → `getBean` 抛 NoSuchBean → 恒系统异常；`prepStandStatus` 永不写入；预站起链路对外不可用 | `UserStandPrepBootstrapServiceImpl` 写 `prepStandStatus`，返回 prepStatus | ⚠ **P1 功能激活**：死功能→可用，会真实触发本手/本轮后站起 |
| 1045 standType=1 | （恒系统异常） | `PARAM_EXCEPTION`（明确不支持，走 1004） | ⚠ 行为变化（dev 本就失败，非回归） |
| 1045 用户不在桌 | （恒系统异常） | `NO_EXIST_TABLE_USER` 业务异常 | ✅ 改进 |
| 1034 留座离桌 | **无 Command** → 静默丢包/超时 | `NOT_SUPPORT_OPERATION(4069)` 明确错误 | ⚠ 行为变化（静默→明确错误，改进） |
| 注释/userId/日志 | 注释错乱、userId 未设、toJson info | 修正、补 userId、脱敏 | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | 补 `UserStandPrepBootstrapServiceImpl`，STAND=1→PARAM_EXCEPTION | ✅ | Impl L24-38 | 一致 |
| R-02 | 1034 新增 NoSupport 命令 | ✅ | `UserHoldSeatCommand` → NOT_SUPPORT_OPERATION | 一致 |
| R-03 | ReqDto/枚举注释纠正 | ✅ | `UserPrepStandReqDto`/`PrepStandStatusEnum` | 一致 |
| R-04 | `after` 补 setUserId | ✅ | `UserPrepStandCommandDefault.after` | 一致 |
| R-05 | 1045 日志收敛 | ✅ | `UserPrepStandCommandDefault` | 一致 |
| R-06 | 接口 Javadoc | ✅ | `UserStandPrepBootstrapService` | 一致 |

**设计 R-01~R-06 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 功能激活 | 1045 预站起由死功能(恒系统异常)激活为可用，会真实在本手/本轮后站起玩家 | dev 无 Impl vs junk `UserStandPrepBootstrapServiceImpl` |
| R-2 | P2 | 行为变化 | 1034 静默丢包→NOT_SUPPORT_OPERATION 明确错误 | `UserHoldSeatCommand`（dev 无此命令） |
| R-3 | 依赖 | 跨范围 | 1045 生效依赖 `PrepStandLeaveCandidateProvider`（未设计 visitor→Provider 重构，见 07 §8） | R051 重命名 |

**无 P0。1 项 P1（R-1）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认** | R-1 | 确认客户端是否在用 1045（含 standType=1）；激活后玩家会被真实站起，需与产品/客户端确认预站起按钮已联调 |
| 可发布 | R-2 | 1034 明确错误优于静默；确认客户端能处理 4069 |
| 依赖 | R-3 | 1045 生效路径依赖未设计的 CandidateProvider 重构，随该专项审计确认 |

## 7. 回归测试覆盖建议
1. 1045 standType=0/2/3 → 写 prepStandStatus，本手/本轮结束被站起。
2. 1045 standType=1 → PARAM_EXCEPTION；用户不在桌 → NO_EXIST_TABLE_USER。
3. 站起/退出后 `BaseUserStandFinishedListener`/`BaseUserQuitFinishedListener` 清回 prepStandStatus=0。
4. 1034 发包 → NOT_SUPPORT_OPERATION，不再超时。
5. 兼容性：UserPrepStandReqDto/RespDto 字段与 dev 一致。

## 8. 相邻改动未审核（附录）
- `PrepStandLeaveUserVisitor`→`PrepStandLeaveCandidateProvider`、`PrepKickLeaveUserVisitor`→`PrepKickLeaveCandidateProvider`：属 07 已标注的**未设计 visitor→CandidateProvider 重构**。1045 预站起的实际"站起"动作依赖该 Provider，需随该专项一并审计等价性。

## 9. 待与产品/业务确认
- **（P1）客户端是否在用 1045 预站起 / standType=1**：激活后行为实质变化。
- 客户端是否在发 1034；留座离桌是否属掼蛋范围（决定后续是否实现）。
- 预站起 check 改用 `commonCheckUserLeave` 与否（等 04/06/07 LeaveIntent 统一）。
