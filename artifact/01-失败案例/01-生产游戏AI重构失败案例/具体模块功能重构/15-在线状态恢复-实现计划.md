# 在线状态恢复实现计划

> 对应设计：`docs/Junk-游戏准备重构/15-在线状态恢复-重构设计.md`
> 工程：dx-game-guandan
> 分支：20260615/junk
> 协议红线：3016 `UserOnlineStatusMsgDto`、16019、14023 字段 0 改动

## 顺序

```
R-01(空值兜底, 核心修P0-1, 先行) ─→ R-02(16019范围明确, 待产品)
```

建议序：`R-01 →（R-02 待产品）`。每个 R 项 = 窄 commit + 一条变更记录。

---

## R-01：UserTableItem 缺失兜底（P0-1，核心修复）
- **目标**：`GdUserOnlineStatusQueryCommand`（`:52-58`）、新增 `GdOnlineStatusQueryService` + `OnlineStatusSnapshotService`/`OnlineStatusSnapshot`
- **步骤**：
  1. 当前用户 `getUserTableItem==null` → 返回明确业务错误或默认在线状态（待产品定）；不再直接 `getUserId/getOnlineStatus` NPE。
  2. 遍历桌内用户跳过缺失项 + warn + 指标，记入 `OnlineStatusSnapshot.missingUserIds`；单个缺失不中断整批补发。
  3. 命令委托 `GdOnlineStatusQueryService.query`。
- **测试**：当前用户/桌内某玩家缺失 → 不系统异常、不中断；在线/网络不佳/掉线状态正确；空玩家列表。
- **回滚**：恢复命令内直接读取。

## R-02：16019 补偿范围明确（P1-1，待产品）
- **步骤**：本期保持现状（每玩家收到自己状态），文档明确语义；若产品要整桌恢复，新增**可选**列表字段或对请求用户循环发各玩家状态，保旧字段兼容。`OnlineStatusNoticeService` 封装策略。
- **P1-2**：文档明确 3016 只恢复 PLAYER_USER；旁观纳入待产品，扩 WATCH_USER 时保兼容。
- **测试**：16019 当前单人语义回归；（若做整桌）请求用户收到各玩家状态。
- **回滚**：保留逐玩家通知。

## 验证与回归
- 无编译环境：静态对照 + 后续 IDE 编译。
- 关键回归：当前用户/桌内玩家缺失兜底、在线状态三态、3016与14023口径一致、心跳变化读最新 onlineStatus。

## 变更记录要求
每个 R 项落代码后在 `git变更记录.md` 补一条（任务序号 15 / 在线状态恢复），窄 commit + 关联 R 编号；与 14 重连、框架 14023/BaseUserOnlineService 的边界在"与其他 Junk 同步要求"标注。
