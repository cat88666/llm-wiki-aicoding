# 入座 结果审核

> 审核日期：2026-06-04
> 待审核分支：frame 20260615/junk @ d5343c65e + guandan @ de39dcd5a
> 基线分支：origin/20260615/dev（frame @ 0c4f47375 / guandan @ 73b3653b3）
> 协议入口：1001 `UserSitDownCommand`（TABLE）；伪协议 1016 占座入座；通知 14001/14022
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无残留 P0；P1×2=HALL/RACE 新增拒绝 + 限流顺序变，待确认；校验链拆分经核对与 dev 等价；P0 并发/空值反被修复）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-045 ~ 056（frame+guandan 12 条）
- frame 范围内：`UserSitDownCommand`/`UserSitDownContext`/`UserSitDownReqDto`/`BaseUserSitDownCheckService`/`BaseUserSitDownDefault`/`Club|Hall|Private|RaceUserSitDownDefault`/`UserSitDownDefault`(M)；新增 `SitDownValidatorChain`+`Seat/SpecifyKick/TableAuth/RiskSitDownValidator`+`ISitDownValidationRule/Extension`+`ISitDownValidatorContributor`+`IOccupySitDownHandler`+`IUserSitDownHandler`；删 6 个旧 handler
- guandan 范围内：`GdSitDownStateService`/`GdSitDownValidatorContributor`(A)、`UserSitDownBootstrap`/`UserSitDownFinishListener`(M)

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1001 | 请求 | `UserSitDownReqDto` | **无**（文件由压缩单行重排为多行；`chairId`/`onlyCheck` 字段保留，onlyCheck 仅可能加 @Deprecated） | 完全兼容 |
| 1001 | 响应 | `UserSitDownRespDto` | 无 | 完全兼容 |
| 1016 | 伪协议 | 占座入座 | 无 | 完全兼容 |
| 14001/14022 | 通知 | `SitdownNoticeDto`/`OccupyAddChipNoticeDto` | 无 | 完全兼容 |

**协议字段 0 改动。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 入座校验项与顺序 | `checkSitDown`：checkSpecifyKick→checkSeat→checkTableAuth→checkRiskSitDown | `SitDownValidatorChain` 4 validator @Order 100/200/300/400，调用**同一批** check，同序 + 扩展在后 | ✅ **等价**（拆分不改顺序/检查项） |
| HALL/RACE 入座 | `handle()` 空方法 → 静默成功无响应 | `throw NOT_SUPPORT_OPERATION` | ⚠ **P1 行为变化**（静默→明确拒绝） |
| 限流 vs 筹码顺序（R-04） | checkSitDown→checkSentinel→checkChipScore | validate→checkChipScore→checkSentinel | ⚠ **P1 错误码顺序变**（余额不足不再消耗限流令牌；balance 错误先于 sentinel） |
| 占座入座 wallet（R-01） | `getNextBringInChipScore` 无空保护 NPE | 空值兜底（保持 OCCUPY 等待重试） | ✅ P0 修复 |
| 占座超时任务注册（R-03） | 在通知 try 块末尾，注册失败卡 OCCUPY | 先注册超时任务再发通知 | ✅ P0 修复 |
| 占座转入座并发（R-09） | WATCH→PLAYER 迁移非原子，EventManager 与主动入座并发 | `GdSitDownStateService`/`IOccupyResitHandler` 表级锁串行化 | ✅ P0 修复 |
| 日志 | GsonUtils.toJson info | 降 debug | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 备注 |
|---|---|---|---|
| R-01 | 占座入座 wallet 空保护 | ✅ | `UserSitDownDefault` |
| R-02 | HALL/RACE 抛 NOT_SUPPORT_OPERATION | ✅ | Hall/Race UserSitDownDefault.handle |
| R-03 | 占座超时任务先注册 | ✅ | `BaseOccupyUserService`（相邻 02 范围） |
| R-04 | 限流移到筹码校验后 | ✅ | `ClubUserSitDownDefault` validate→chipScore→sentinel |
| R-06 | Handler 委托、消除重复 | ✅ | 删 BaseUserSitDownHandler 等 6 handler |
| R-09 | 占座转入座表级锁 | ✅ | `GdSitDownStateService` impl `IOccupyResitHandler` |
| 校验链 | 设计称"本期不强拆" | **超出设计**：实际全拆为 4 validator | 偏离设计但经核对**行为等价**（同序同检查） |

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 新增拒绝 | HALL/RACE 1001 由静默成功→NOT_SUPPORT_OPERATION | Hall/Race handle dev 空 vs junk throw |
| R-2 | **P1** | 错误码顺序 | 限流移到筹码校验后，余额不足/限流错误先后顺序变 | `ClubUserSitDownDefault` L44/47 |
| R-3 | 设计偏离 | 范围 | 校验链全拆（设计称不拆），经核对等价 | @Order 100-400 vs dev checkSitDown |
| R-4 | P2 | 测试 | 删除的 handler 仅被 `UserSitDownCommandMockInitData`(M) 引用，需确认测试已同步 | diff 测试文件为 M |
| R-5 | 改进(原P0) | 并发/空值 | R-01/R-03/R-09 修复占座 NPE/卡死/并发 | 见 §3 |

**无残留 P0；2 项 P1（R-1/R-2）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认** | R-1 | 确认客户端不向 HALL/RACE 桌发 1001；NOT_SUPPORT_OPERATION(4069) 客户端有文案 |
| **灰度观察** | R-2 | 确认限流/余额错误码顺序变化对客户端无副作用（余额不足→充值→重试不再被限流拦） |
| 可发布 | R-3/R-5 | 校验链等价、并发修复，建议占座并发回归 |
| 验证 | R-4 | IDE 编译确认测试 mock 已同步删除的 handler 引用 |

## 7. 回归测试覆盖建议
1. CLUB/PRIVATE 入座：4 项校验顺序与 dev 一致；正常入座成功。
2. HALL/RACE 1001 → NOT_SUPPORT_OPERATION，不再静默。
3. 余额不足→充值→重试：限流令牌未被提前消耗，可立即入座（R-04）。
4. 占座转入座并发：EventManager 与主动入座并发，表级锁防双 PLAYER（R-09）。
5. 占座 wallet 缺失：不 NPE，保持 OCCUPY 等待（R-01）；通知失败超时任务已注册（R-03）。
6. 兼容性：1001/14001/14022 字段与 dev 一致。

## 8. 相邻改动未审核（附录）
- `RiskSitDownValidator` 复用 08 `BaseTableRiskService.checkRiskSitDown`（08 已审，注意 08 的"规则失败保守拦截"P1 会经此 validator 在入座链生效）。
- `BaseOccupyUserService`（R-03）若不在本 diff 行号范围，归占座专项确认。

## 9. 待与产品/业务确认
- （P1）HALL/RACE 入座拒绝、限流顺序变更是否影响线上客户端。
- 08 风控"规则失败保守拦截"经 RiskSitDownValidator 在入座生效，与 08 §9 同一待确认项联动。
- `onlyCheck`/`BaseUserSitDownHandler` @Deprecated 后最终删除时机（麻将等其它游戏是否仍继承）。

## 10. 补充修正（2026-06-05，本次审核遗漏的 P0）

> 本审核初版核对了校验链 @Order 顺序等价，但**遗漏核对 R-09 占座转入座表级锁编排类是否存在**。Phase 3 实施 03 时发现：

| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| **R-6** | **P0(编译断裂)** | 缺类 | `UserSitDownBootstrap:11/40/97`、`UserSitDownFinishListener:15/54/113` 引用并调用 `com.dx.game.frame.service.biz.sitdown.SitDownStateService.occupyToSitDown`，但该类在 frame/guandan/origin **均不存在**（疑似 move/simplify handler 重构漏提交），占座转入座链路编译断裂 | `ls`/`find`/`git cat-file -e origin` 均确认缺失；全仓无 `occupyToSitDown` 方法定义 |

**处理**：已按 02 设计补建 `SitDownStateService`（frame `service.biz.sitdown`，表级 ReentrantLock + 回调 `IOccupySitDownHandler`，仅 `occupyToSitDown`/`releaseTableLock`），修复编译断裂，commit `480366d57`（frame）。02 整体结论由 ⚠灰度观察 调整为：**编译断裂已修，仍 ⚠灰度观察**（原 P1 行为变化项不变）；建议 IDE 编译 + 占座转入座回归确认。
