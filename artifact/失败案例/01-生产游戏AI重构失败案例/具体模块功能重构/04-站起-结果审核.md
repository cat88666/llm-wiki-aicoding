# 站起 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：1007 `UserStandCommand`（TABLE）→ `UserStandCommandDefault` → `UserStandTask` → `playerStand` → 14007 `UserStandNotice`
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；P0 event key 已修复=改进；chipScore=0 兜底待客户端确认；强制站起路由依赖未设计 CandidateProvider）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-017 ~ 019
- 范围内文件：`UserStandCommandDefault`(M)、`UserStandNotice`(M)、`UserStandTask`(M)
- 相邻（未设计 visitor→Provider 重构 / handler 清理，见 §8）：删 `BaseUserStandService`/`UserStandHander`/`ForcedStandUserVisitor`、`RiskStandLeaveUserVisitor`→`RiskStandLeaveCandidateProvider`

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1007 | 请求/响应 | `UserStandReqDto`/`UserStandRespDto` | 无 | 完全兼容 |
| 14007 | 通知 | `UserStandNoticeDto` | 无（`chipScore` 异常时返回 0） | 完全兼容 |

**协议字段 0 改动（DTO 未在 diff ✓）。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 站起任务 event key | `getKey(USER_QUIT_TASK,..)` 却 publish `UserStandTask` → 与 1006 并发互相覆盖 P0 | `getKey(USER_STAND_TASK,..)` | ✅ **P0 修复**（客户端 1007 入口；07 R-01 修系统站起入口，二者配套） |
| 14007 钱包读取 | `getChipScore` 抛异常 → 通知组装失败 → 客户端/桌内收不到 14007 | `safeFetchChipScore` try/catch → 兜底 0 + warn，通知照发 | ⚠ 行为改进；`chipScore=0` 值语义待客户端确认 |
| `playerStand` 调用契约 | — | 不变 | ✅ 等价 |
| 日志 | context/req/noticeDto toJson info | 降 debug，info 仅元数据 | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | event key USER_QUIT_TASK→USER_STAND_TASK | ✅ | `UserStandCommandDefault` L92（dev L141 为 QUIT） | 一致 |
| R-02 | `UserStandNotice` 钱包 NPE 兜底（0 + warn，不阻断通知） | ✅ | `safeFetchChipScore` L81-87，`setChipScore` L57 | 一致（兜底类型为 `BigDecimal.ZERO`，设计文字误写 Long） |
| R-03 | 1007 链路日志收敛 | ✅ | CommandDefault/Notice/Task | 一致 |

**设计 R-01~R-03 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | 改进(原P0) | 行为修复 | 站起任务 event key 修复，消除与 1006 并发互覆盖（配 07 R-01） | `UserStandCommandDefault` L92 vs dev L141 |
| R-2 | P2 | 行为变化 | 14007 钱包异常时 `chipScore=0` 兜底（通知不再失败） | `safeFetchChipScore` L81-87 |
| R-3 | 依赖 | 跨范围 | 强制站起删 `ForcedStandUserVisitor`，路由改走未设计的 CandidateProvider（见 07 §8） | 删除 + RiskStand→Provider |

**无 P0/P1（04 设计范围内均为修复/改进）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| 可发布 | R-1 | P0 修复，回归并发 1007/1006 event key 独立 |
| 灰度观察 | R-2 | 确认客户端对 14007 `chipScore=0`（钱包异常兜底）显示合理 |
| 依赖单独审计 | R-3 | 强制站起候选人路由随未设计 CandidateProvider 专项审计 |

## 7. 回归测试覆盖建议
1. 同用户并发 1007/1006 → EventManager 两 key（STAND/QUIT）独立不互覆盖。
2. mock `getChipScore` 抛异常 → 14007 仍发出、chipScore=0、warn 定位。
3. 风控站起/强制站起链路 14007 兜底同样生效。
4. 兼容性：1007/14007 字段与 dev 一致。
5. 强制站起候选人筛选（CandidateProvider）专项对比 dev 的 ForcedStandUserVisitor 输出。

## 8. 相邻改动未审核（附录）
- 删 `BaseUserStandService`/`UserStandHander`/`ForcedStandUserVisitor`（均已无引用，删除安全）、`RiskStandLeaveUserVisitor`→`RiskStandLeaveCandidateProvider`：属 07 §8 标注的**未设计 visitor→CandidateProvider 重构**；强制/风控站起候选人逻辑随该专项一并审计等价性。
- `UserStandPrepBootstrapService`/`Impl`：属 18 预站起。

## 9. 待与产品/业务确认
- （P2）14007 `chipScore=0` 钱包异常兜底是否符合客户端展示预期。
- 强制站起 CandidateProvider 与旧 ForcedStandUserVisitor 等价性（未设计重构专项）。
- `LeaveIntent`/`StandRuleChain`（04 §11 转交 06/07）。
