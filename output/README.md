# 提取清单

| 序号 | 来源 | 类型 | 输出 | 说明 |
|---:|---|---|---|---|
| 1 | `raw/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线.ipynb` | `ipynb` | `output/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线.md` | Markdown 单元 41 个，Raw 单元 0 个，跳过代码单元 4 个 |
| 2 | `raw/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线.pdf` | `pdf` | `output/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线/大项目里的 AI：用特征测试做安全重构，用 GitHub 打通协作管线_pdf.md` | PDF 文本层字符数 19658 |
| 3 | `raw/用 SDD + TDD 驯服 AI：让代码既能「写得快」也能「信得过」/用 SDD + TDD 驯服 AI：让代码既能「写得快」也能「信得过」.ipynb` | `ipynb` | `output/用 SDD + TDD 驯服 AI：让代码既能「写得快」也能「信得过」/用 SDD + TDD 驯服 AI：让代码既能「写得快」也能「信得过」.md` | Markdown 单元 23 个，Raw 单元 0 个，跳过代码单元 3 个 |

## 范围说明

本次转换严格限定为 `raw` 目录下的 `.ipynb` 与 `.pdf` 文件。Notebook 仅保留 Markdown/Raw 文本单元，不提取代码单元、执行输出、代码目录或其他格式资源。PDF 仅基于文本层提取，不进行图片 OCR。
