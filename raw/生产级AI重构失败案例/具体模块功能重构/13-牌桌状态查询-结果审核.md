# 牌桌状态查询 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：`com.dx.game.frame.cmd.command.tablestatus.TableStatusCommand`（1025 `CommonProto.TABLE_STATUS`，@ThreadModel PLAYER）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；2 项 P1 行为变化需客户端/产品确认；2 项 NPE 修复为改进）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-030 ~ 031
- 改动文件：`TableStatusCommand`(M，import 包路径)、`TableStatusContext`(M，+`disbanded`)、`TableStatusCommandDefault`(R056 迁包 `enterRoom`→`tablestatus` + 改)

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1025 | 请求 | `TableStatusReqDto` | 无 | 完全兼容 |
| 1025 | 响应 | `TableStatusRespDto` | 无（解散桌取值 status=GAME_END，见 §3） | 完全兼容 |

`TableStatusContext` 仅新增**内部**字段 `disbanded`（不进协议）。**协议字段 0 改动**（DTO 未在 diff ✓）。

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| HALL 桌权限校验 | `if(isHall){Hall}` + `if(isPrivate)else{Club}` → **Hall + Club 都跑** | `if(isHall){Hall} else if...` → **仅 Hall** | ⚠ **行为变化**：HALL 不再跑 Club 校验，对「Club 拒/ Hall 过」的边缘用户变宽松 |
| RACE 桌 | else→Club | else→Club | ✅ 不变 |
| CLUB 桌 | else→Club | else→Club | ✅ 不变 |
| PRIVATE 桌 | if(isPrivate)→Private | else if(isPrivate)→Private | ✅ 等价 |
| 解散桌 | `initContext` 不 set status → `after` `getTableStatus().getCode()` **NPE → SYSTEM_EXCEPTION** | `disbanded=true`+`GAME_END`，check/refreshUser/after 短路，返回 status=GAME_END(2) | ⚠ **行为变化**：异常→明确 GAME_END |
| tableInfo==null | `BeanUtils.copyProperties(null,..)` / `getTableId()` NPE | 空 `TableSimpleCfgDto` 兜底 + warn | ✅ 改进（NPE→兜底） |
| 日志 | handleCommand/initContext/after 三处大对象 toJson info | 降 debug，主链路精简 info | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | check() 互斥分支修 HALL 重复/RACE 失语义 | ✅ | `check` L130-136（if/else if/else） | 一致 |
| R-02 | disband → GAME_END + 全链路短路 | ✅ | `initContext` L88-91、`check` L117、`refreshUser` L141、`after` L163-166 | 一致 |
| R-03 | refreshUser disband 短路 | ✅ | `refreshUser` L141 | 一致 |
| R-03 | after tableInfo==null 兜底 | ✅ | `after` L177 else 分支 | 一致 |
| R-04 | 日志收敛 | ✅ | handleCommand/initContext/after | 一致 |
| Context | 新增内部 `disbanded` | ✅ | `TableStatusContext` L29 | 一致 |

**设计 R-01~R-04 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 行为变化（变宽松） | HALL 桌不再跑 Club 校验（dev 跑 Hall+Club） | dev `check` L228-242 vs junk L130-136 |
| R-2 | **P1** | 行为变化（客户端感知） | 解散桌由 SYSTEM_EXCEPTION 改为返回 status=GAME_END(2) | junk `initContext` L88-91 + `after` L163-166 |
| R-3 | P2 | 行为变化 | tableInfo==null 由 NPE 改为空配置兜底 | `after` L177 |
| R-4 | 观察 | 待产品 | RACE 桌仍走 Club 校验（未改），是否应走 Hall 待确认 | `check` else 分支 |
| R-5 | 观察 | 跨范围 | `GameTableBootstrapService.init` 查询副作用、Enum 统一本期不处理 | 设计 §6 |

**无 P0。2 项 P1（R-1/R-2）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认** | R-2 | 客户端确认收到 1025 `status=GAME_END` 能正确处理（替代原 SYSTEM_EXCEPTION） |
| **灰度观察** | R-1 | 确认 HALL 桌本不应受 Club 规则约束；灰度观察 HALL 1025 通过率变化 |
| 待产品 | R-4 | RACE 桌权限校验语义（Club vs Hall） |

## 7. 回归测试覆盖建议
1. **HALL 桌**：1025 只跑 `checkUserCanJoinRoomHall`，不再额外跑 Club。
2. **PRIVATE / CLUB / RACE**：分别命中 Private / Club / Club 校验，行为同 dev（RACE 标注待确认）。
3. **解散桌**：mock `checkDisbandTable=true` → 返回 status=GAME_END(2) + 空配置 + isPassword=false，不抛系统异常。
4. **tableInfo 缺失**：mock getTableInfo=null → 返回 status + 空配置 + warn。
5. **兼容性**：正常 1025 响应字段与 dev 完全一致。

## 8. 相邻改动未审核（附录）
- `TableStatusCommandDefault` 迁包 commit 同批含 12 EnterRoomBefore、Leave/Stand 等改动，分别归各自功能。

## 9. 待与产品/业务确认
- **（P1）解散桌返回 GAME_END(2) 而非系统异常**：客户端确认可正确处理。
- **（P1）HALL 桌去除 Club 校验**：确认 HALL 不应受俱乐部规则约束。
- RACE 桌是否应走 Hall 校验；`TableStatusEnum`/`GameStatusEnum` 是否统一；`GameTableBootstrapService.init` 查询副作用是否保留（均单独立项）。
