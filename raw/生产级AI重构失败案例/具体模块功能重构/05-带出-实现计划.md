# 带出实现计划

> 对应设计：`docs/Junk-游戏准备重构/05-带出-重构设计.md`
> 工程：dx-game-frame + dx-game-guandan
> 分支：20260615/junk
> 协议红线：1023/1024/14009 字段 0 改动；`bringOutStatus` 枚举语义不变

## 顺序

```
R-02(入口NPE) 独立先行(纯防御)
R-01(PolicyResolver) ─→ AutoBringOutService/BringOutCommandService 复用
R-03(校验规则链) 依赖 R-01 场景
R-04(成功builder锁测试) 谨慎，仅补测试不改行为
R-05(钱包/通知封装+日志) 贯穿
```

建议序：`R-02 → R-01 → R-03 → R-05 →（R-04 仅锁测试）`。每个 R 项 = 窄 commit + 一条变更记录。

---

## R-02：入口 NPE 修复（P0-2，先行）
- **目标文件**：`UserBringOutDefault.java`、`UserBringOutCommandDefault.java`、`UserBringOutViewCommandDefault.java`
- **步骤**：① `tableHand` 读取移入 `initContext`，`handCode = tableHand==null?"-":tableHand.getHandCode()`；② `:83` `getTableHand(tableId).getTableHandStatus()` 前判空；③ 先 `checkDisbandTable`/`BaseTableInfoCache` 再组装日志。
- **测试**：mock getTableHand=null → 1023/1024 返回业务异常，不 NPE；catch 日志 handCode="-"。
- **回滚**：恢复 try 前直接读 tableHand。

## R-01：`BringOutPolicyResolver` 决策（P0-1）
- **新增**：`BringOutPolicy{applyTiming(CURRENT/NEXT_HAND/RETAIN/SKIP), walletAmounts, retainAmount, reason}` + `BringOutPolicyResolver`（frame）+ `AutoBringOutService`
- **步骤**：① 把 `UserBringOutBootstrap.autoBringOut` 的 `shouldCurrentBringOutForSettleStand/...ForOwnerForcedLeave/...ForPrivateAuditSettleLeave/...ForRoundFeeActiveLeave` 收敛为按优先级规则表；② `reason` 对齐 07 `LeaveIntent`；③ 自动带出入口（`GdUserLeaveBootstrapService.playerQuit`/彩金/强制）改调 `AutoBringOutService.auto(tableId,userId,intent)`。
- **测试**：7 种 reason × timing 单测；退出/结算站起/私审/强制/轮费/彩金 决策正确。
- **回滚**：恢复 autoBringOut 长 if 链。
- **⚠ 跨功能**：reason 依赖 07 `LeaveIntent`，回归退出/站起自动带出。

## R-03：校验规则链化（P1-1）
- **新增**：`BringOutValidatorChain` + `CommonBringOutAmountRule`/`PrivateFixedKeepRule`/`TableLimitRule`
- **步骤**：拆 `check` 为 3 规则；掼蛋覆盖类改为追加 `PrivateFixedKeepRule`，保留 `TableLimitRule`（`checkBringOutTableLimit`）不被绕过。
- **测试**：私人固定房保留 + 表级限制都生效；普通房金额/精度/最小保留。
- **回滚**：恢复覆盖类整段校验。

## R-04：成功响应 builder 锁兼容（P1-2，谨慎，本期仅补测试）
- **步骤**：补测试锁定 `UserBringOutDefault.sendBringOutResp :118/120` 的 `buildErrorResponse(SUCCESS,...)` 实际报文结构 + 客户端解析；**本期不改 builder**。`BringOutNoticeService` 封装当前行为。
- **测试**：1024 成功响应报文与 dev 完全一致（字段/code/msg）。
- **回滚**：N/A（未改行为）。
- **⚠ 待确认**：改正常 buildResponse 需客户端确认（设计 §10）。

## R-05：钱包/通知封装 + 日志脱敏（P2/D6）
- **新增**：`BringOutWalletService`（DownScoreReq + 主钱包/彩金列表 + 本地缓存同步可重试）、`BringOutNoticeService`（1024 + 14009）
- **步骤**：明确 downScore→本地同步→通知 顺序；资金 toJson info→debug，info 仅 tableId/userId/reason/applyTiming/金额。
- **测试**：下分成功后本地同步 + 14009；同步失败可重试。
- **回滚**：日志/封装还原。

## 验证与回归
- 无编译环境：静态对照 dev + 后续 IDE 编译。
- 关键回归：手动当前手/游戏中下一手、退出自动带出主钱包+彩金、结算特殊离场不误判、无 tableHand 业务异常、私人固定房保留+表级限制、1024/14009 字段与 dev 一致。
- 影子对比：PolicyResolver 输出 vs 旧分支一致后切流。

## 变更记录要求
每个 R 项落代码后在 `git变更记录.md` 补一条（任务序号 5 / 带出），窄 commit + 关联 R 编号；reason 对齐 07 LeaveIntent 的改动在"与其他 Junk 同步要求"标注。
