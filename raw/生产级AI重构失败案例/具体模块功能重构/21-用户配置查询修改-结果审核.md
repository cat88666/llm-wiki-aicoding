# 用户配置查询/修改 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e；dx-game-guandan 20260615/junk @ e276babfa
> 基线分支：origin/20260615/dev（frame @ 0c4f47375 / guandan @ 73b3653b3）
> 协议入口：1028/1029 `UserConfigQueryCommandDefault`/`UserConfigUpdateCommandDefault`（frame）；3018/3019 `GdUserConfigQueryCommand`/`GdUserConfigUpdateCommand`（guandan）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；存在 1 项 P1 行为收紧需产品确认 + 埋点；其余为错误路径优雅降级改进）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-036 ~ 037
- **纠正指导备注**：指导曾标「仅 1028/1029，3018/3019 待确认」。实际 diff 显示 `GdUserConfigQueryCommand` / `GdUserConfigUpdateCommand` **均已改造**，3018/3019 已落地（R-02/R-03 确在代码中）。
- 改动文件：frame `UserConfigQueryCommandDefault`/`UserConfigUpdateCommandDefault`(M) + `AutoComplementConfigService`/`UserConfigCacheService`(A)；guandan `GdUserConfigQueryCommand`/`GdUserConfigUpdateCommand`(M)

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1028 | 请求/响应 | `UserConfigQueryReqDto`/`UserConfigQueryRespDto` | 无 | 完全兼容 |
| 1029 | 请求/响应 | `UserConfigUpdateReqDto` | 无（字段不变） | 完全兼容（**但取值校验收紧，见 §3 R-1**） |
| 3018 | 请求/响应 | `GdUserConfigQueryReq`/`GdUserConfigQueryRespDto` | 无 | 完全兼容 |
| 3019 | 请求/响应 | `GdUserConfigUpdateReq` | 无 | 完全兼容 |
| 14050 | 通知 | `UserConfigNoticeMsgDto` | 无 | 完全兼容 |

**协议号/DTO 字段 0 改动**（diff 未触及任何 DTO 文件 ✓）。

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 自动补码目标值校验（1029） | 仅要求**正数** | `saveIfPresent(...,requireInteger=true)` 要求**正整数** | ⚠ **P1 收紧**：dev 接受的正非整数（如 150.5）junk 抛 PARAM_EXCEPTION |
| 自动补码口径（1029 vs 3019） | 两路径各自实现，口径不同 | 统一走 `AutoComplementConfigService`，均 `requireInteger=true` | ✅ 口径统一（消除 P0 不一致） |
| 3018 RPC 返回 null | `BeanUtils.copyProperties(null,..)` → NPE → SYSTEM_EXCEPTION | `==null` 时 warn + 返回空 respDto(带 userId) | ⚠ 行为改进：错误→优雅降级 |
| 3019 `configContent=null` | `JSONUtil.parseObj(null)` 可能异常、多刷一次 | 兜底 `new JSONObject()` | ⚠ 行为改进：异常→空 JSON |
| 缓存刷新语义 | invalidate 后 getUnchecked，异常上抛 | `UserConfigCacheService.refresh` 返回 boolean，异常吞+记录 | ✅ 等价+显式化 |
| 日志 | 全链路 `GsonUtils.toJson(req/resp)` info | 降 debug，保留关键字段 info；错码字符串 1041→1029 修正 | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | 抽 `AutoComplementConfigService`，1029/3019 共用、统一正整数 | ✅ | `AutoComplementConfigService.saveIfPresent(..,true)`；1029 L59-60、3019 L102 | 一致 |
| R-02 | 3019 `configContent` 空值兜底 | ✅ | `GdUserConfigUpdateCommand` L58-60 | 一致 |
| R-03 | 3018 RPC 空值保护 | ✅ | `GdUserConfigQueryCommand` L40-43 | 一致 |
| R-04 | `UserConfigCacheService.refresh` 显式 boolean | ✅ | frame service；1029 L87、3019 L128 | 一致 |
| R-05 | 1028/1029/3018/3019 日志收敛 | ✅ | 四个命令类 | 一致 |

**设计 R-01~R-05 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 新增校验/业务收紧 | 1029 `autoBringChipScore` 由「正数」收紧为「正整数」，dev 接受的非整数请求将被拒 | `AutoComplementConfigService.isValidRequest`（scale 校验）+ 1029 `saveIfPresent(..,true)` |
| R-2 | P2 | 行为变化 | 3018 RPC null 由 SYSTEM_EXCEPTION 改为返回空 respDto | `GdUserConfigQueryCommand` L40-43 |
| R-3 | P2 | 行为变化 | 3019 configContent null 由可能异常改为空 JSON | `GdUserConfigUpdateCommand` L58-60 |
| R-4 | 观察 | 跨范围 | `UserConfigCache` 其它静态调用方（结算/补码/退出）未迁移，按设计本期不动 | 设计 §9 |

**无 P0。1 项 P1（R-1）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认 + 灰度埋点** | R-1 | 确认掼蛋/桌面客户端是否会发非整数 `autoBringChipScore`；灰度阶段埋点观察 1029 PARAM_EXCEPTION 比例，无异常增长再全量 |
| 灰度观察 | R-2/R-3 | 优雅降级，确认前端能处理「空配置响应」而非依赖旧的失败/异常 |

## 7. 回归测试覆盖建议
1. **正常**：1029/3019 设 `autoBringChipScore=200`，按 [min,max] 夹取写 Redis，两路径一致。
2. **并发**：同 userId 并发 3019 改开关 + refresh，缓存最终一致。
3. **异常**：3018 mock RPC 返回 null → 客户端得 userId 非空、configContent=null，不抛系统异常。
4. **重连/兼容**：3019 仅传 `configMap.autoBringChipScore` → resolveFromConfigMap 解析成功。
5. **兼容性（P1 重点）**：1029 传 `autoBringChipScore=150.5` → 期望 PARAM_EXCEPTION（dev 为接受）；需产品确认此收紧可接受。

## 8. 相邻改动未审核（附录）
- 无（本功能 4 文件均在范围内）。

## 9. 待与产品/业务确认
- **（P1）1029 `autoBringChipScore` 收紧为正整数是否可接受**：若线上掼蛋客户端仍用 1029 且可能发非整数，需先确认或灰度埋点。
- 14050 是否需要携带 `autoBringChipScore`（当前仅 `userId/autoComplement`）。
- 目标值「写 0 作为删除语义」是否长期可接受（`TableUserRedisService` 无 delete）。
