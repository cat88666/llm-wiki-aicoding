# 退出 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：1005 `UserQuitCommand`（PLAYER）→ `UserQuitCommandDefault` → `UserQuitTask` → `playerQuit` → 1005/14008 `UserQuitNotice`
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无残留 P0；本次修复 1 个 P0 NPE=改进；null/0 兜底值待客户端确认；协议 0 改动）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-020 ~ 022
- 范围内文件：`UserQuitNotice`(M)、`UserQuitCommandDefault`(M)、`UserQuitTask`(M)
- 相邻：删 `UserQuitHandler`（handler 路由清理，无引用，删除安全）

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1005 | 请求 | `UserQuitReqDto` | 无 | 完全兼容 |
| 1005/14008 | 响应/通知 | `UserQuitNoticeDto` | 无（`chairId`/`status`/`chipScore` 在 userItem/钱包缺失时为 null/0） | 完全兼容 |

**协议字段 0 改动（DTO 未在 diff ✓）。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| userItem 缺失（重复退出/已清理后再 1005） | `getUser` 返回 null → `userItem.getChairId()`/`getStatus()` **必然 NPE** → 栈崩 | `userItem==null` → chairId/status=null，照发 1005/14008 | ✅ **P0 NPE 修复**（dev L67/69 无空保护） |
| 钱包缺失 | `getChipScore` 抛异常 → 14008 失败 | `safeFetchChipScore`→ZERO + warn | ✅ 改进（与 04 R-02 同模式） |
| 正常退出 PLAYER→QUIT | 原逻辑 | 不变 | ✅ 等价 |
| 断线退出 handType=1/2 | `breakLinkForTable` | 不变 | ✅ 等价 |
| 日志 | req/context/userItem/noticeDto toJson info | 降 debug，info 仅元数据 | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | `UserQuitNotice` userItem 空值保护（chairId/status=null 幂等） | ✅ | `UserQuitNotice` L49/51 | 一致 |
| R-02 | 钱包读取 NPE 兜底 ZERO | ✅ | `safeFetchChipScore` L83-88，`setChipScore` L52 | 一致 |
| R-03 | 1005 链路日志收敛 | ✅ | CommandDefault/Task/Notice | 一致 |

**设计 R-01~R-03 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | 改进(原P0) | NPE 修复 | 缺失 userItem 的 1005（重复退出/已清理）由必然 NPE 改为幂等成功 | dev `UserQuitNotice` L67/69 无空保护 vs junk L49/51 |
| R-2 | P2 | 行为变化 | userItem/钱包缺失时 14008 `chairId/status=null`、`chipScore=0` 兜底 | `UserQuitNotice` L49-52 |
| R-3 | 改进 | 删除安全 | 删 `UserQuitHandler`（无引用） | grep 0 引用 |

**无残留 P0/P1（本次实为修复 dev 的 P0 NPE）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| 可发布 | R-1 | P0 修复，回归重复退出 / userItem 缺失 / 钱包缺失场景 |
| 灰度观察 | R-2 | 确认客户端对 14008 `chairId/status=null`、`chipScore=0`（缺失兜底）展示合理 |

## 7. 回归测试覆盖建议
1. userItem 缺失：mock getUser=null → 1005/14008 发出、chairId/status=null/chipScore=0、不 NPE。
2. 钱包缺失：mock getChipScore 抛异常 → 14008 发出、chipScore=0、warn。
3. 正常退出 PLAYER→QUIT，字段与 dev 一致。
4. 重复退出：连续 2 次 1005，第二次不再 NPE，返回成功。
5. 断线退出 handType=1/2 走 breakLinkForTable，行为不变。

## 8. 相邻改动未审核（附录）
- 删 `UserQuitHandler`（无引用，handler 路由简化的一部分）；强制踢出/离开候选人体系见 07 §8 未设计 CandidateProvider 重构。

## 9. 待与产品/业务确认
- （P2）14008 `chairId=null/status=null/chipScore=0` 缺失兜底是否符合客户端展示预期。
- `LeaveIntent`/`LeaveRuleChain`/`QuitFlowService`/`QuitSnapshot` 大改（06 §11 转交专项，涉及 04/05/06/07）。
- `handType=1/2` 断线协议语义、强制踢出通知顺序（06 §13 待确认，本期不动）。
