# Junk 游戏准备重构 — 待确认清单（全量汇总）

> 汇总 21 个功能 审核(A组)/ 设计(B组) 中所有待确认项 + 跨负责人契约 + 已知潜在 bug。
> 目的：供产品 / 客户端 / 技术 一次性拍板，解除阻塞、降低 Phase 3 结构性编码返工。
> 状态口径：每项标注 类型、来源功能、本期已取的安全默认、影响。
> 生成日期：2026-06-05　分支：20260615/junk

## 0. ★阻塞型（必须先拍板，否则结构性编码会返工）

| 编号 | 待确认 | 来源 | 安全默认 | 决策方 |
|---|---|---|---|---|
| BLK-1 | **1042 放弃补码** 废弃 vs 实现 | 17 | 实现薄命令路由 TRANSFER 站起 + 失效审核单（否则 NoSupport） | 产品/客户端 |
| BLK-2 | **1012/1013 旁观返回/重连** 废弃 vs 适配 | 14 | 重连以 1000 为准；适配薄命令或 NoSupport | 产品/客户端 |
| BLK-3 | **审核单是否新增 PROCESSING/PASS_APPLIED 中间态**（决定审核幂等用 CAS 还是钱包 eventId） | 11 | 优先 Redis CAS，无中间态则钱包 eventId 幂等 | 技术/DBA |
| BLK-4 | **审核回调 MQ 是否保证同一 auditId 有序** | 03/11 | 默认无序 → 必做 Redis CAS | 技术 |
| BLK-5 | **建单成功但 MQ 失败** 客户端应见成功还是失败 | 10 | "申请已受理" + 补发，不回退已建单 | 产品/客户端 |
| BLK-6 | **3005 实时账单主块失败** 由半空成功改为错误响应 | 09 | 改错误响应（已落码 e38368151） | 客户端 |

## 1. 产品决策（业务语义 / 拦截策略）

| 来源 | 待确认 | 安全默认 |
|---|---|---|
| 08 风控 | 风控规则计算**异常时** 放行 vs 保守拦截 | 保守拦截 TABLE_RISK_ERROR + 灰度埋点（已落码方向） |
| 08 风控 | REALTIME_VOICE_RISK 是否纳入风险优先级 | 维持现状不纳入 |
| 12 进房前校验 | 私人房禁入(privateMajongStatus)由失效转生效=新增拦截 | 生效（修复）+ 灰度观察拦截量 |
| 13 牌桌状态查询 | HALL 桌去除冗余 Club 校验是否符合预期 | 去除（HALL 不受 Club 规则） |
| 13 牌桌状态查询 | RACE 桌权限校验走 Club vs Hall | 维持 Club（同 dev） |
| 21 用户配置 | 1029 autoBringChipScore **正数→正整数收紧** | 收紧 + 灰度埋点 PARAM_EXCEPTION 比例 |
| 16 固定带入审核 | hasPendingTransferAudit 恢复 keep 判断 | 恢复（已落码 32d7c033f，修待审核被超时误站起） |
| 16 固定带入审核 | 固定带入审核是否只首次入座 / 结算重坐也适用 | 首次 + 重新坐下额度申请 |
| 10 私人房申请审核 | 用户不在 OCCUPY 但仍在房间时是否允许额度申请 | 默认要求 OCCUPY/补码等待（本期仅空值守卫，状态门禁待定） |
| 11 私人房审核通过 | 审核通过但用户已离桌：自动上分再带出 vs 作废 | 沿用现状，显式化策略 |
| 11 私人房审核通过 | 拒绝/超时后用户是否必须立即站起 | 沿用现状 |
| 05 带出 | 私人固定带入保留筹码是否覆盖所有自动带出场景 | PrivateFixedKeepRule 追加 |
| 05 带出 | 彩金结算与自动带出先后顺序是否有账变审计要求 | 维持现状顺序 |
| 09 实时账单 | 钱包汇总 RPC 失败时金额显示 0 / 隐藏 / 上次快照 | 现状(0/空) + status 进指标 |
| 09 实时账单 | 总带入为 0 跳过是否适用房主/旁观 | 维持现状跳过 |
| 09 实时账单 | 局数回退手数是否仍符合产品口径 | 保留 FALLBACK |
| 20 好友备注 | 负缓存 60s TTL 是否满足"他端设备注→本端可见"预期 | 60s |
| 17 放弃补码 | 放弃后用户站起为旁观 vs 退出房间 | 沿用 TRANSFER 站起 |
| 15 在线状态恢复 | 旁观者是否需要在线状态恢复 | 仅 PLAYER_USER |

## 2. 客户端协议（兼容/展示语义）

| 来源 | 待确认 | 安全默认 |
|---|---|---|
| 02 入座 | HALL/RACE 1001 静默成功→NOT_SUPPORT_OPERATION，客户端是否会发 | 抛 NOT_SUPPORT（已落码） |
| 02 入座 | 限流移到筹码校验后，余额/限流错误码先后变化 | 已落码（余额不足不消耗限流令牌） |
| 04 站起 | 14007 钱包异常时 chipScore=0 兜底展示是否合理 | 0（已落码方向） |
| 06 退出 | 14008 chairId=null/status=null/chipScore=0 缺失兜底展示 | null/0（已落码方向） |
| 13 牌桌状态查询 | 解散桌返回 status=GAME_END(2) 替代 SYSTEM_EXCEPTION | GAME_END |
| 18 预站起 | 客户端是否仍用 1045 / standType=1 | 1045 激活；standType=1→PARAM_EXCEPTION 走 1004 |
| 18 预站起 | 1034 留座离桌静默→NOT_SUPPORT_OPERATION | NoSupport |
| 18 预站起 | 是否需要 14039；14007 是否足够 | 不强制 14039 |
| 19 风控数据上报 | UserRiskInfoDto 新增可选 riskTypeList 字段，新客户端解析 | 可选字段（老客户端忽略） |
| 20 好友备注 | RPC 异常返回部分命中而非空 | 部分命中（改进） |
| 21 用户配置 | 14050 是否需携带 autoBringChipScore | 本期不扩展 |
| 12 进房前校验 | needPwd 是否回传客户端；roomNo 是否仍用 | 不回传/不动 |
| 16 固定带入审核 | 前端是否依赖 addChipType 以外字段区分额度申请 | 保持 addChipType=SIT_DOWN 语义 |
| 15 在线状态恢复 | 16019 单人状态 vs 整桌快照补偿 | 单人语义（已落码） |
| 15 在线状态恢复 | 当前用户不在 PLAYER_USER 时 3016 返回策略 | 默认在线/不NPE（已落码） |

## 3. 技术 / 跨负责人 / 结构性

| 来源 | 待确认 | 处理 |
|---|---|---|
| ★04/06/07/18 | **visitor→CandidateProvider 重构无设计文档、已接入** BaseUserLeaveService/UserLeaveDefault | 需补设计 + 单独立项审计等价性（见 07 §8） |
| ★全局 | **SitDownStateService 曾缺失致编译断裂**（02 R-09 漏提交） | 已补建 frame 480366d57；确认是否原有 jar 版本 |
| 10/11/16 | 房主审核动作归 **Ceres** | 仅记 MQ/审核单接口契约，不重构 |
| 03 带入 | GD_BONUS 审核单是否允许新增更准确外部字段（替代 bringInSquidAmount） | 保留 squid + 内部 ReviewWalletAmount 视图（已落码 7224609aa） |
| 03 带入/结算 | playerIsContinue 0/1 语义是否与前端完全一致 | 沿用现状，标注 |
| 03/05 | 自动补至 vs 补入 文案区分 | 内部 scene 已区分，文案待产品 |
| 05 带出 | buildErrorResponse 构造成功响应是否历史协议兼容要求 | 本期保留 + 锁测试，改正常 builder 待确认 |
| 05 带出 | NEXT_HAND_SUCCESS_ROUND_FEE 是否仍被客户端使用 | 保留枚举 |
| 13 牌桌状态查询 | TableStatusEnum/GameStatusEnum 是否统一 | 单独立项 |
| 13 牌桌状态查询 | GameTableBootstrapService.init 查询副作用是否保留 | 单独立项 |
| 11 | 钱包 RPC 是否支持以审核单 id 幂等 | 决定资金幂等实现 |
| 14 重连 | 重启后重连数据是否需强恢复(Redis/重放) | 现状 transient + 标注 D1 风险 |
| 09 实时账单 | 3016 是否需限流（同类） | 本期不加 |

## 4. ★已知潜在 bug（未擅改，待团队确认）

| 位置 | 问题 | 建议 |
|---|---|---|
| `UserBringInDefault:591` | `bringInTotal = getBringInAmount() + getBringInAmount()`（两次主筹码，疑似复制粘贴漏 `getBringInSquidAmount`），导致总额=2×主筹码 | 团队确认 bringInTotal 用途后修正；ReviewWalletAmount 视图(已落码)可消除此类直读 |
| 18 / 20 | commit `036cfd439` 混合 18 R-01 与 20 S-02 | 审核已拆清，后续提交避免混合 |
| A组多处 | "实施 100%" 登记由后补、原作者非本人 | 审核以 dev diff 为唯一事实源（已遵循） |

---

> 维护：本清单随产品拍板逐项关闭（标注结论 + 关闭日期）；阻塞项(BLK-*)关闭前对应结构性编码不应全量上线。
