# 风控数据上报 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：1040 `RiskDataCollectionCommand`（PLAYER）/ 1041 `RiskDataGraphCommand`（TABLE）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：✅ **可发布**（无 P0/P1；1 项协议观察项=新增可选字段；P0 跨桌串用已修复）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-009 ~ 012
- **19 范围内文件**：`UserRiskCache`、`RiskDataCollectionCommandDefault`、`RiskDataGraphCommandDefault`、`RiskDataCollectionCommand`、`RiskDataGraphCommand`、`UserRiskInfoDto`、新增 `RiskDataRedisSyncService`/`RiskGraphAssembler`
- **相邻改动（属 08/04/07，列附录）**：`BaseTableRiskService`、`RiskCheckResult/RuleFailure/RiskTableSnapshot`、`RiskPolicyRegistry`、`EnterRoomRiskService`、`RiskSitDownValidator`、`RiskStandLeaveCandidateProvider`、删 `UserRiskGraphHandler`

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1040 | 请求/响应 | `RiskDataCollectionReqDto`/`RiskDataCollectionDto` | 无 | 完全兼容 |
| 1041 | 请求/响应 | `RiskDataGraphReqDto`/`RiskDataGraphRespDto` | 无 | 完全兼容 |
| 1041 | 子 DTO | `UserRiskInfoDto` | **新增可选** `riskTypeList: List<Integer>` | ⚠ **协议观察项**：新增可选字段，老客户端忽略，向后兼容 |

**主 DTO 0 改动；唯一变化是 1 个新增可选字段（铁律允许，非 P0）。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 风控缓存 key | `LoadingCache<Long(userId), ..>` 跨桌/换桌串用（P0） | `RiskDataCacheKey(tableId,userId)` 隔离 | ✅ **P0 修复**（数据正确性提升；旧 userId-key 条目 3h TTL 自然淘汰） |
| 1040 Redis 同步 | `syncRiskDataToRedis` 与主响应耦合，异常→1040 系统错误 | `RiskDataRedisSyncService.syncSafely` try/catch warn，不影响主响应 | ✅ 改进（解耦） |
| 1041 GPS 空值 | `getLatitude().doubleValue()` GPS 缺失 NPE | `isGpsValid` 短路 + 空保护 | ✅ 改进（消 NPE） |
| 1041 风险类型 | 仅 IP/GPS 三规则 | 复用 `BaseTableRiskService` 全规则 + `riskTypeList` | ✅ 增强（与 08 一致；新内容承载于新增可选字段） |
| 日志 | IP/GPS/整桌风险 info 明文 | 降 debug，info 仅元数据 | ✅ 脱敏改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | 缓存 key 加 table 维度 | ✅ | `UserRiskCache.RiskDataCacheKey` L44 + `LoadingCache<RiskDataCacheKey,..>` L49 | 一致 |
| R-02 | 1040 主响应与 Redis 同步解耦 | ✅ | `RiskDataCollectionCommandDefault` L33 `syncSafely`；新增 `RiskDataRedisSyncService` | 一致 |
| R-03 | 1041 GPS 空保护 + 规则统一 | ✅ | 新增 `RiskGraphAssembler.assemble`；`RiskDataGraphCommandDefault` L29 委托 | 一致 |
| R-04 | `UserRiskInfoDto` 新增可选 `riskTypeList` | ✅ | `UserRiskInfoDto`（List<Integer> 可空） | 一致 |
| R-05 | 日志收敛/脱敏 | ✅ | 命令类 + UserRiskCache | 一致 |

**设计 R-01~R-05 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | 观察项 | 协议（新增可选字段） | `UserRiskInfoDto.riskTypeList` 新增；需新客户端按可选解析 | `UserRiskInfoDto` diff |
| R-2 | 改进(原P0) | 行为修复 | 风控缓存由 userId-key 改 (tableId,userId)，消除跨桌串用 | `UserRiskCache` L44/49/64/75 |
| R-3 | 观察 | 跨功能纠缠 | 本批 commit 含 08 风控引擎重构（BaseTableRiskService 等），归 08 审核 | diff 附录 |

**无 P0/P1。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| 可发布 | R-1 | 确认新客户端按可选字段解析 `riskTypeList`；老客户端无感 |
| 可发布 | R-2 | P0 修复，建议回归跨桌场景确认隔离 |
| 依赖 08 | R-3 | 1041 `riskTypeList` 内容依赖 08 `BaseTableRiskService`，随 08 审核确认 |

## 7. 回归测试覆盖建议
1. **正常**：1040 上报 → 1041 关系图正常返回。
2. **并发**：同桌多用户并发 1040/1041，缓存无串桌。
3. **异常**：mock Redis `syncRiskData` 抛异常 → 1040 仍正常响应（仅 warn）。
4. **重连/边界**：GPS 缺失玩家 1041 不 NPE，距离空、GPS_NOT_OPEN 正确判定。
5. **兼容性**：跨桌 A 在 T1/T2 上报不同 IP，`getUserRiskData(T1,A)`/`(T2,A)` 独立；老客户端忽略 `riskTypeList`。

## 8. 相邻改动未审核（附录，归 08/04/07）
- `BaseTableRiskService`（M）、`RiskCheckResult`/`RuleFailure`/`RiskTableSnapshot`（A）、`RiskPolicyRegistry`（A）、`EnterRoomRiskService`（A）→ **08 风控**
- `RiskSitDownValidator`（A）→ **02 入座 / 08 风控**
- `RiskStandLeaveCandidateProvider`（R071，原 `RiskStandLeaveUserVisitor`）→ **04 站起 / 07 离开**
- 删 `UserRiskGraphHandler` → 随 1041 瘦身，确认无其它引用

## 9. 待与产品/业务确认
- `riskTypeList` 是否需要前端版本协同（新字段展示）。
- 1041 `riskTypeList` 全规则内容正确性依赖 08 风控引擎审核结论。
