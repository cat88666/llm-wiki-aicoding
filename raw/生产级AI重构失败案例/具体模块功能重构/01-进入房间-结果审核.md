# 进入房间 结果审核

> 审核日期：2026-06-04
> 待审核分支：frame 20260615/junk @ d5343c65e + guandan @ 29fb01546
> 基线分支：origin/20260615/dev（frame @ 0c4f47375 / guandan @ 73b3653b3）
> 协议入口：1000 `EnterRoomCommand`（PLAYER，原 `EnterTableCommand` 重命名）→ `EnterBaseRoomHandlerDefault` → ValidatorChain/StateService/SnapshotAssembler
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；P0 私人房计数已修复；HALL/RACE handler 删除经核对安全；R-08 异常带出改同步=时序变化待确认；协议 0 改动）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260603-006/007/008 + GIT-20260604-057~068（共 15 commit）
- frame 范围：`EnterRoomCommand`(R086 重命名)、`EnterRoomContextV2`(M 字段重命名)、`EnterBaseRoomHandlerDefault`(A，原 EnterBaseTableHandlerDefault D)、`EnterClubRoomHandlerDefault`(R068)、`EnterPrivateTableHandlerDefault`(M)、新增 ValidatorChain/StateService/SnapshotAssembler/EnterRoomRiskService + validators(Common/Club/Private) + 接口(IEnterRoomValidationExtension/Rule/IEnterRoomHandAssembler/IReconnectStateHandler/IEnterRoomHandler)；删 EnterHall/RaceTableHandlerDefault + 4 旧 handler；`BaseTableCheckService`/`BaseUserEnterRoomFinishListener`(M)
- guandan 范围：`GdEnterRoomHandAssembler`/`GdEnterRoomValidationExtension`/`GdReconnectStateService`(A)

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1000 | 请求 | `EnterRoomReqDto` | 无 | 完全兼容 |
| 1000 | 响应 | `EnterRoomRespDtoV2` | 无 | 完全兼容 |
| 14015 | 通知 | `NewUserMsgDto` | 无 | 完全兼容 |

`EnterRoomContextV2.isNewRoomUser`→`alreadyInRoom` 为**内部 context** 字段重命名（非协议 DTO）。**协议字段 0 改动。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 私人房人数上限（R-01） | `incrementAndGet`「校验即递增」，失败不补偿→可能错误判满 | WATCH_USER.size()+PLAYER_USER.size() 实际集合计数 | ✅ **P0 修复**（计数更正确） |
| **HALL/RACE 进房** | `EnterHallTableHandlerDefault` 仅委托 `EnterBaseTableHandlerDefault.handleData` | 删除该 handler → 工厂 `getEnterRoomHandler` 对未注册空间**回退 CLUB**（EnterClubRoomHandlerDefault extends EnterBaseRoomHandlerDefault） | ✅ **等价**（CLUB 回退 = dev 的瘦委托同一基类逻辑） |
| 进房校验项 | 8 个静态校验散在 handleData | `EnterRoomValidatorChain` 内置 8 校验同序 + 空间扩展(Club/Private validator) | ✅ 等价（顺序/检查项保留） |
| 状态写入并发 | PLAYER 线程写 WATCH_USER 与 TABLE 并发不安全 | `EnterRoomStateService` 表级 ReentrantLock 串行化 | ✅ P0/P1 修复 |
| 空值保护（R-02） | wallet/tableHand/raceGlobalConfig 无空保护→重连系统异常 | 全部空值兜底 | ✅ 修复 |
| 异常带出（R-08） | `BaseUserEnterRoomFinishListener` 异步事件（吞异常） | `after()` 同步直接调用 | ⚠ **时序变化**（异步→同步，可靠但进房响应路径耦合带出） |
| 日志 | GsonUtils.toJson(context) info | 降 debug | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 备注 |
|---|---|---|---|
| R-01 | 私人房计数集合 size | ✅ | `BaseTableCheckService` L72-77 |
| R-02 | wallet/tableHand/raceGlobalConfig 空保护 | ✅ | SnapshotAssembler/GdEnterRoomHandAssembler/TableUserHandBootstrap |
| R-03 | 日志降级 | ✅ | EnterBaseRoomHandlerDefault |
| R-08 | 异常带出剥离监听改 after 同步 | ✅ | `after()` + Listener 移除分支 |
| 架构 | FlowService/ValidatorChain/StateService/SnapshotAssembler + 3 Gd 扩展 | ✅(偏离) | 无独立 `EnterRoomFlowService`，编排留在 `EnterBaseRoomHandlerDefault`；功能等价 |
| 重命名 | isNewRoomUser→alreadyInRoom 全量 | ✅ | 内部 context |

**设计 R-01~R-08 落地；编排未拆独立 FlowService（偏离设计，功能等价）。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | 改进(原P0) | 计数修复 | 私人房人数由「校验即递增」改实际集合计数 | `BaseTableCheckService` L72 |
| R-2 | **核对通过** | 删除安全 | 删 HALL/RACE 进房 handler，工厂 CLUB 回退等价（dev 本就瘦委托同基类） | dev EnterHallTableHandlerDefault L46 delegate vs 工厂 ENTER_ROOM CLUB 回退 |
| R-3 | P1 | 时序变化 | 异常带出由异步监听改 after() 同步，耦合进房响应路径 | `after()` R-08 |
| R-4 | P2 | 提交卫生 | 4 个「重构：进入房间优化」commit(065~068) message 笼统，需补 R 编号 | git 变更记录 |
| R-5 | 改进(原P1) | 并发 | 表级锁串行化 WATCH_USER 写入 | `EnterRoomStateService` |

**无残留 P0（私人房计数 P0 已修）。1 项 P1（R-3 时序）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| 可发布 | R-1/R-2/R-5 | P0 计数修复 + HALL/RACE CLUB 回退等价 + 并发锁，建议四空间进房 + 私人房并发回归 |
| **灰度观察** | R-3 | 确认异常带出改同步后不显著拖慢进房响应；失败有日志/指标不静默 |
| 追溯 | R-4 | 补 065~068 commit 的 R 编号/polish 说明 |

## 7. 回归测试覆盖建议
1. CLUB/PRIVATE/HALL/RACE 四空间进房：HALL/RACE 走 CLUB 回退，行为与 dev 一致。
2. 私人房重复进房并发：WATCH_USER 只出现一次，人数上限不重复增加（R-01）。
3. 游戏中重连：取消托管、清预退出/预站起、手牌快照正确（GdReconnectStateService/GdEnterRoomHandAssembler）。
4. 缓存缺失容错：wallet/TableHand/raceGlobalConfig 缺失返回明确业务异常，不系统异常（R-02）。
5. 异常带出：进房成功后异常带出同步执行，失败有日志/指标（R-08）。
6. 兼容性：1000/14015 字段与 dev 一致。

## 8. 相邻改动未审核（附录）
- `EnterRoomRiskService`(A) 风控进房接入——与 08 风控联动，随 08 结论。
- `TableStatusCommandDefault`(R056) 在本批迁包——归 13 牌桌状态查询（已审）。
- `EnterRoomBefore*` 不在本范围——归 12（已审）。

## 9. 待与产品/业务确认
- （P1）异常带出由异步改同步的时序影响（进房响应耗时 / 失败处理）。
- 065~068 笼统 commit 的实际改动归属 R 编号（原作者补充）。
- HALL/RACE 进房走 CLUB 回退是否长期符合产品预期（与 13 牌桌状态查询 RACE 走 Club 同类待确认）。
