# 离开状态检查 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：无直接命令协议；公共能力 `UserLeaveBootstrapService.commonCheckUserLeave/commonCheckUserBreakLink` + `UserLeaveDefault`（被 1005/1007/1045 及系统站起消费）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（07 设计范围 R-01~R-04 干净、协议 0 改动；但同批存在**未设计、已接入**的 visitor→CandidateProvider 重构需单独审计）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-023 ~ 026
- **07 设计范围内**：`UserLeaveDefault`(M)、新增 `LeaveIntent`/`UserArea`/`LeaveDecision`/`LeaveDecisionService`
- **同批超出 07 设计的改动**（见 §8）：`UserLeaveCandidateDispatcher`(A)、`*CandidateProvider`(R from visitor)、删 `UserLeaveVisitorDispatcher`/`UserLeaveVisitor`/`ForcedStandUserVisitor`、`BaseUserLeaveService`(M)、`PrepStandStatusEnum`(M)

## 2. 协议层结论
| 协议/接口 | 变化 | 兼容性 |
|---|---|---|
| 1001/1005/1007/1045 字段 | 无 | 完全兼容 |
| `commonCheckUserLeave`/`commonCheckUserBreakLink` 签名+返回 | 无 | 完全兼容 |
| `UserLeaveDefault.userStand/pushQuitTask/playerQuit/playerStand` 签名 | 无 | 完全兼容 |
| `BaseUserLeaveService.OK_LEAVE_LIST` | 无 | 完全兼容 |
| 新增 `LeaveDecisionService.evaluate` | 新 API | opt-in，老代码无感 |

**无客户端协议字段变化。**

## 3. 业务等价性结论（07 设计范围，对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 系统站起 event key（R-01） | `getKey(USER_QUIT_TASK,..)` 却 `publish(UserStandTask)` → key 错配 P0 | `getKey(USER_STAND_TASK,..)` | ✅ **P0 修复**（与 04 R-01 同型，系统站起入口补修） |
| pushQuitTask event key | `USER_QUIT_TASK`（正确） | `USER_QUIT_TASK`（不变） | ✅ 等价 |
| `commonCheckUserLeave` 行为 | 原逻辑 | 不变（仅日志降级） | ✅ 等价 |
| `LeaveDecisionService` | 不存在 | 新增、**无任何既有调用方引用** | ✅ 纯 opt-in，零行为变化 |
| 日志 | context toJson info | 降 debug | ✅ 改进 |

## 4. 设计文档落地核对（07 范围）
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | userStand event key USER_QUIT_TASK→USER_STAND_TASK | ✅ | `UserLeaveDefault` L181（带注释） | 一致 |
| R-02 | `LeaveIntent`/`UserArea`/`LeaveDecision` 新增 | ✅ | constant.enums.user / model.dto.leave | 一致 |
| R-03 | `LeaveDecisionService` opt-in | ✅ | service.biz.userleave；无既有引用 | 一致 |
| R-04 | 日志收敛 | ✅ | `UserLeaveDefault` | 一致 |

**07 设计 R-01~R-04 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | 改进(原P0) | 行为修复 | 系统站起 event key 错配修复（08风控/补码失败/房主强制站起受益） | `UserLeaveDefault` L181 vs dev L285 |
| **R-2** | **待审(潜在P1)** | **未设计的已接入重构** | visitor→CandidateProvider 体系（删 3 visitor，新增 Dispatcher+Provider）已接入 BaseUserLeaveService/UserLeaveDefault，**无设计文档、未验证等价性** | `UserLeaveCandidateDispatcher` 被 `BaseUserLeaveService`/`UserLeaveDefault` 引用 |
| R-3 | 观察 | opt-in | `LeaveDecisionService`/`LeaveDecision` 暂无调用方，价值待后续迁移兑现 | grep 无引用 |

**07 设计范围内无 P0/P1；R-2 是范围外的未设计重构，单独立项。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| 可发布 | R-1 | P0 修复，回归 08 风控站起 / 房主强制站起 / 并发 1006 event key |
| **必须单独审计** | R-2 | visitor→CandidateProvider 重构补设计文档 + 对 dev 等价性审计（强制站起/踢人/预站起候选人逻辑），归 04 站起 / 06 退出 或新立"LeaveCandidate"专项 |

## 7. 回归测试覆盖建议
1. **R-1**：08 风控触发系统站起 → event key=`...USER_STAND...`；并发系统站起+客户端 1006 不互相覆盖。
2. **等价**：1005 退出 / 1007 站起 / 1045 预站起 校验行为与 dev 一致（commonCheckUserLeave 不变）。
3. **opt-in**：确认 LeaveDecisionService 当前不在任何主链路（grep 0 引用）。
4. **R-2 专项**：强制站起/踢人/预站起的候选人筛选，新 CandidateProvider 与旧 Visitor 输出逐场景对比。

## 8. 相邻改动未审核（附录）★需单独立项
- **visitor→CandidateProvider 重构（未设计、已接入）**：`UserLeaveCandidateDispatcher`、`ForcedLeaveCandidateProvider`、`PrepKickLeaveCandidateProvider`、`PrepStandLeaveCandidateProvider`、`RiskStandLeaveCandidateProvider`、`UserLeaveCandidateProvider`；删除 `UserLeaveVisitorDispatcher`/`UserLeaveVisitor`/`ForcedStandUserVisitor`。这是 04 站起/06 退出/18 预站起/08 风控站起共用的强制离开候选人体系，**无对应 xx-重构设计.md**，本次不在 07 范围内放行。
- `PrepStandStatusEnum`(M)、`BaseUserLeaveService`(M) → 关联 18 预站起 / 上述候选人体系。

## 9. 待与产品/业务确认
- visitor→CandidateProvider 重构是否需补设计文档并单独审计（强烈建议）。
- `commonCheckUserLeave` `@Deprecated` 时机、LeaveRuleChain 8+ 规则拆分（07 §11 转交专项）。
