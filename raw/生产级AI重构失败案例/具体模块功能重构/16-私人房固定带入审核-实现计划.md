# 私人房固定带入审核实现计划

> 对应设计：`docs/Junk-游戏准备重构/16-私人房固定带入审核-重构设计.md`
> 工程：dx-game-guandan
> 分支：20260615/junk
> 协议红线：入座/带入/1078、`needAmountApply`、`OCCUPY_ADD_CHIPS_NOTICE`、`addChipType` 语义 0 改动

## 顺序

```
R-02(pending audit keep, 核心修复) 可优先(独立、修真实风险)
R-01(Policy 收口) ─→ R-03(addChipType 统一)
```

建议序：`R-02 → R-01 → R-03`。每个 R 项 = 窄 commit + 一条变更记录。

---

## R-02：待审核单纳入 keep 判断（P0-2，核心修复，优先）
- **目标**：`SettleComplementAuditService.shouldKeepPrivateAuditUserForAmountApply`、新增 `PrivateAuditPendingService`
- **步骤**：恢复 `needAmountApply=YES || hasPendingTransferAudit(tableId,userId)`（`hasPendingTransferAudit` 封装到 `PrivateAuditPendingService`，判 Redis 未过期审核单）；重复审核单/等待审核单分别日志。
- **测试**：已提交 1078 待审核 + viewResp 临时不返回 needAmountApply → 仍保留占座，不被补码超时任务站起。
- **回滚**：恢复仅 needAmountApply 判断。
- **⚠ 待确认**：恢复 hasPendingTransferAudit 待产品确认（设计 §10），但本期默认恢复修风险。

## R-01：场景识别收口 `PrivateFixedBringInAuditPolicy`（P0-1）
- **新增**：`PrivateFixedBringInAuditPolicy` + `PrivateAuditDecision{needAudit,hasPendingAudit,keepOccupy,noticeAddChipType,suppressToast,reason}`
- **步骤**：`UserSitDownBootstrap`、`UserSitDownFinishListener.resendOccupyAddChipNotice`、`UserBringInBootstrap.getCanBringChipScore` 改调 policy，删除各自重复分支；needAmountApply 判定统一来源。
- **测试**：私人审核房非房主固定带入 needAudit；房主/非审核房不需要；三入口结果一致。
- **回滚**：恢复各入口内联分支。

## R-03：addChipType 由策略统一输出（P1-1）
- **新增**：`PrivateAuditNoticeService`
- **步骤**：`PrivateAuditDecision.noticeAddChipType` 重新坐下额度申请固定 `AddChipTypeEnum.SIT_DOWN`；`PrivateAuditNoticeService` 按 decision 发 `OCCUPY_ADD_CHIPS_NOTICE`(suppressToast)，避免额度申请与剩余筹码过少 toast 叠加。
- **测试**：重新坐下额度申请 addChipType=SIT_DOWN，无普通补码 toast；结算补码分支正确。
- **回滚**：恢复 sendSitDownAmountApplyNoticeWithoutToast 内联。

## 验证与回归
- 无编译环境：静态对照 + 后续 IDE 编译。
- 关键回归：固定带入非房主触发审核、房主不触发、彩金固定带入金额进审核单、**提交1078后不被超时站起(P0-2)**、重新坐下 addChipType=SIT_DOWN、审核通过继续开局。

## 变更记录要求
每个 R 项落代码后在 `git变更记录.md` 补一条（任务序号 16 / 私人房固定带入审核），窄 commit + 关联 R 编号；与 02 入座/03 带入/10 建单/11 审核回调 的边界在"与其他 Junk 同步要求"标注。
