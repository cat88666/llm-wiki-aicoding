# 进入房间前校验 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：`com.dx.game.frame.cmd.command.enterroom.EnterRoomBeforeCommand`（1032 `CommonProto.USER_BEFORE_ENTER_ROOM`，@ThreadModel PLAYER）
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；1 项 P1：私人房禁入校验由失效转生效=新增拦截，需产品/风控确认；密码基线零回归已验证）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-027 ~ 029（frame：`d8443cab2` R-01、`904efde88` R-02/03/04、`e9b9c6993` R-02/05）
- 改动文件：`EnterRoomBeforeCommand`(M，仅 import 包路径)、`EnterRoomBeforeCommandDefault`(A，旧路径 D 删除)、`GameHandleFactory`(新增 getEnterRoomBeforeHandler)、`PrivatedEnterRoomBeforeHandler`(R 重命名迁包+改)、`Base/Club/HallEnterRoomBeforeHandler`(R 重命名迁包)、`service/handler/IEnterRoomBeforeHandler`(A)
- 说明：frame 近期 4 个 commit 把 enterRoomBefore handler 从 `service.biz.handler.enterroombefore` 迁到 `service.biz.bootstrap.command.enterRoomBefore`，接口 `IEnterRoomBeforeHandler` 提到 `service.handler`（纯迁包，不改语义）

## 2. 协议层结论
| 协议号 | 类型 | DTO | 字段变化 | 兼容性 |
|---|---|---|---|---|
| 1032 | 请求 | `EnterRoomBeforeReqDto`（tableEntryPassword/roomNo） | 无 | 完全兼容 |
| 1032 | 响应 | `EnterRoomBeforeRespDto`（tableId） | 无 | 完全兼容 |

**协议 0 改动**（diff 未触及 DTO ✓）。

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 默认链路分发 | `check()` 直调 `checkUserPwd`，**从不分发空间 handler** | `check()` → `GameHandleFactory.getEnterRoomBeforeHandler` 分发 | 见下分项 |
| CLUB/HALL/RACE 密码校验 | checkUserPwd | Club/Hall handler `super.check()`=checkUserPwd | ✅ 等价 |
| 私人房密码校验 | checkUserPwd（直接） | Private handler `super.check()`=checkUserPwd | ✅ 等价（密码零回归） |
| **私人房禁入校验** | `PrivatedEnterRoomBeforeHandler` 存在但**从未被调用**，`privateMajongStatus` 禁入**不生效** | 现经分发执行，`privateMajongStatus==1` → 抛 `USER_JOIN_GAME_LOCK_STATUS` | ⚠ **新增拦截=业务变化**（dev 无此拦截） |
| 无匹配空间/tableInfo==null | （dev 不分发，恒走 checkUserPwd） | handler==null → 兜底 checkUserPwd | ✅ 等价、不 NPE |
| needPwd/roomNo | needPwd 计算不用、roomNo 不校验 | 保留 context 计算、不新增强制校验（不改协议） | ✅ 等价 |
| 日志 | `GsonUtils.toJson(context)` info | 降 debug，主链路精简 info | ✅ 改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | `GameHandleFactory.getEnterRoomBeforeHandler` additive | ✅ | `GameHandleFactory` L50（无匹配/tableInfo==null 返回 null） | 一致 |
| R-02 | `check()` 走 handler 分发 + 兜底 | ✅ | `EnterRoomBeforeCommandDefault.check` L74-82 | 一致 |
| R-02 | Private handler 补 `super.check()` 密码基线 | ✅ | `PrivatedEnterRoomBeforeHandler.handle` L26 | 一致 |
| R-03 | needPwd/roomNo 语义保留不改协议 | ✅ | `initContext` 保留计算 | 一致 |
| R-04 | 日志降级 | ✅ | `handleCommand`/`initContext` debug + 精简 info | 一致 |
| R-05 | Private 日志文案修正（去"德州"） | ✅ | "玩家账号被禁止进入私人房" | 一致 |

**设计 R-01~R-05 全部落地。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 新增禁入校验=业务变化 | 私人房 `privateMajongStatus==1` 玩家由「可入」变「被 1032 拦截」 | `PrivatedEnterRoomBeforeHandler.handle` L30-31；dev 中该 handler 从未被分发 |
| R-2 | 观察 | 跨功能依赖 | 复用 01 进入房间的 `GameHandleFactory` 分发，本次仅 additive，未影响 01/02 既有分发 | `GameHandleFactory.getEnterRoomBeforeHandler` |

**无 P0。1 项 P1（R-1）。密码基线零回归已验证。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认 + 灰度** | R-1 | 与产品/风控确认私人房禁入拦截应当生效、无依赖旧「不拦截」行为的灰度玩家；灰度阶段观察 `USER_JOIN_GAME_LOCK_STATUS` 在 1032 的触发量 |

## 7. 回归测试覆盖建议
1. **正常**：CLUB 密码房正确/错误密码 → 行为同 dev。
2. **修复验证**：PRIVATE 桌 `privateMajongStatus=1` 玩家发 1032 → 抛 `USER_JOIN_GAME_LOCK_STATUS`（dev 漏拦）。
3. **零回归**：PRIVATE 桌正常玩家 + 正确密码 → 返回 tableId。
4. **异常/兜底**：`tableInfo==null` 或新增未注册空间 → 走兜底 checkUserPwd，不 NPE。
5. **阶段交叉**：1032 成功 → 1000 进房正常。

## 8. 相邻改动未审核（附录）
- handler 迁包的 4 个 commit 同时含 13 牌桌状态、04/06/07 等 Leave/Stand 相关改动，分别归各自功能审核。

## 9. 待与产品/业务确认
- **（P1）私人房禁入校验由失效转生效**：确认 `privateMajongStatus==1` 玩家应被 1032 拦截、线上无依赖旧「不生效」行为。
- needPwd 是否需回传客户端；roomNo 是否仍被客户端使用（本次未动，保持兼容）。
