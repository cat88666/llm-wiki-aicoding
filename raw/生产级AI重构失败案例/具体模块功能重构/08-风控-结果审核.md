# 风控 结果审核

> 审核日期：2026-06-04
> 待审核分支：dx-game-frame 20260615/junk @ d5343c65e（本功能 frame-only，内部服务无客户端协议）
> 基线分支：dx-game-frame origin/20260615/dev @ 0c4f47375
> 协议入口：无直接命令协议；核心 `BaseTableRiskService`（入座校验 / 实时轮询 / 1041关系图 / 强制站起 消费）；通知 14038 `RISK_CHANGE_NOTICE`
> 前置文档：扫描 / 设计 / 实现计划 / git 变更记录（已读）
> 审核结论：⚠ **灰度观察**（无 P0；1 项 P1：风控规则计算异常由「放行」改为「保守拦截」=行为反转，需产品确认；P0 异常掩盖/快照不一致已修）

## 1. 审核准入检查
- 任务完成度：扫描100% 设计100% 计划100% 实施100% 审核-未审核 → ✓ 可审核
- 关联 commit：GIT-20260604-013 ~ 016
- 范围内文件：`BaseTableRiskService`(M)、新增 `RiskCheckResult`/`RuleFailure`/`RiskTableSnapshot`(model)、`RiskPolicyRegistry`(service)
- 与 19 共享：19 已引入 `(tableId,userId)` 缓存维度；本期 1041 `RiskGraphAssembler` 经 `checkRiskData→checkRiskDataResult` 自动获益

## 2. 协议层结论
| 协议/接口 | 变化 | 兼容性 |
|---|---|---|
| 14038 `RISK_CHANGE_NOTICE` / `UserRiskNoticeDto` | 无 | 完全兼容 |
| 1007 站起 / 14007 | 无 | 完全兼容 |
| 强制站起 `SpecifyLeaveUserDto` | 无 | 完全兼容 |
| 入座抛错码（`TABLE_RISK_ERROR` 等） | 错误码不变；新增「规则失败→TABLE_RISK_ERROR」 | 客户端文案兼容 |
| `BaseTableRiskService` public static 方法 | 签名/返回类型全保留（`checkRiskData` @Deprecated 委托） | 调用方零改动 |

**无客户端协议字段变化（内部服务重构）。**

## 3. 业务等价性结论（对 dev 基线）
| 检查点 | dev 基线 | junk 当前 | 等价性 |
|---|---|---|---|
| 风控规则计算异常 | `try{5规则}catch{return riskMap}` → 吞异常返回(空)map → **放行** | 每规则 `safeRule` 独立 try，任一失败 `success=false` | 见下 |
| 入座规则失败 | map 空 → `getUserRiskType`=CHECK_NO_RISK → **通过** | `!success` → 抛 `TABLE_RISK_ERROR` → **拦截** | ⚠ **P1 行为反转**（异常时 dev 放行 / junk 拦截） |
| 风险优先级顺序 | `RISK_PRIORITY_ORDER`=[VIDEO,DEVICE,GPS_NOT_OPEN,GPS_EQUAL,IP_EQUAL] | `RiskPolicyRegistry.PRIORITY_ORDER` **完全相同** | ✅ 等价 |
| `getRiskReason`/StandType 映射 | 内联 switch | `RiskPolicyRegistry` 委托，映射相同 | ✅ 等价 |
| realtimeCheckRisk 数据一致性 | 多次独立读 cache（race：算到的风险用户 ≠ 通知接收人） | `RiskTableSnapshot.capture` 一次快照贯穿 | ✅ P0 修复（边缘场景行为更正确） |
| 二次确认数据源 | 站起后重读 cache | 复用 snapshot.riskData（有意为之，避免重复处理） | ⚠ 行为变化（设计 §11 待确认） |
| 日志 | tableInfo/riskData/riskMap 全 JSON info（含 IP/GPS） | 降 debug，info 仅元数据 | ✅ 脱敏改进 |

## 4. 设计文档落地核对
| 编号 | 设计描述 | 是否落地 | 实现位置 | 一致性 |
|---|---|---|---|---|
| R-01 | `RiskCheckResult` + 每规则独立 try，异常不静默成无风险 | ✅ | `checkRiskDataResult` L92 + `safeRule`；`checkRiskData` @Deprecated L128-130 | 一致 |
| R-01 | 入座保守拦截 | ✅ | `checkRiskSitDown` L75-78（!isSuccess→TABLE_RISK_ERROR） | 一致 |
| R-02 | `RiskTableSnapshot` 一次快照贯穿 realtimeCheckRisk | ✅ | `realtimeCheckRisk` L157 + computeNeedStandUserIds/sendCheckRiskNotice 用 snapshot | 一致 |
| R-03 | `RiskPolicyRegistry` 集中优先级/Reason/StandType | ✅ | `RiskPolicyRegistry`（PRIORITY_ORDER 与 dev 同序） | 一致 |
| R-04 | 日志收敛/IP·GPS 脱敏 | ✅ | checkRiskDataResult/realtimeCheckRisk/sendCheckRiskNotice | 一致 |

**设计 R-01~R-04 全部落地；`RiskActionPlanner` 按设计本期不做（留 04/18）。**

## 5. 风险汇总表
| 编号 | 严重度 | 类别 | 一句话 | 证据 |
|---|---|---|---|---|
| R-1 | **P1** | 行为反转 | 风控规则计算异常时：dev 放行 → junk 保守拦截(TABLE_RISK_ERROR) | dev `checkRiskData` catch return map vs junk `checkRiskSitDown` L75-78 |
| R-2 | 改进(原P0) | 数据一致性 | realtimeCheckRisk 由多次读 cache 改为一次快照，消除通知/站起对象错配 | `RiskTableSnapshot.capture` |
| R-3 | P2 | 行为变化 | 站起后二次确认复用 snapshot.riskData 而非重读 cache | `realtimeCheckRisk` L181 |
| R-4 | 观察 | 待产品 | `REALTIME_VOICE_RISK` 仍未纳入优先级（dev 亦无，非本次引入） | `PRIORITY_ORDER` |

**无 P0。1 项 P1（R-1）。**

## 6. 上线建议
| 档位 | 风险编号 | 修复方向 |
|---|---|---|
| **必须确认 + 灰度** | R-1 | 产品确认「风控规则计算失败时应拦截而非放行」；灰度观察 `TABLE_RISK_ERROR` 因规则失败触发的量，防止规则偶发异常导致大面积误拦 |
| 灰度观察 | R-3 | 确认二次确认用旧快照符合预期（避免刚站起又看到新上报重复处理） |
| 待产品 | R-4 | REALTIME_VOICE_RISK 是否纳入优先级 |

## 7. 回归测试覆盖建议
1. **异常拦截（P1 重点）**：mock `getGpsRiskData` 抛 NPE → 入座被 `TABLE_RISK_ERROR` 拦截，日志见 `failedRules:[GPS_EQUAL]`（dev 为放行）。
2. **一致快照**：realtimeCheckRisk 过程中一名 player 退出 → 不向已退出用户发 14038；中途入座 player 不进 needStandUsers。
3. **优先级等价**：多风险命中时 `getUserRiskType` 取最高优先级，结果与 dev 一致。
4. **getRiskReason 等价**：5 类 + null + UNKNOWN → KickerReason 与 dev 一致。
5. **兼容性**：`checkRiskData`(@Deprecated) 旧调用方返回 map 行为不变；14038 字段不变。

## 8. 相邻改动未审核（附录）
- `RiskSitDownValidator`（02 入座/08 交叉，新增）、`RiskStandLeaveCandidateProvider`（04/07）、`EnterRoomRiskService`（01/08）——这些是风控被消费侧的接入，分别归对应功能审核。

## 9. 待与产品/业务确认
- **（P1）风控规则计算失败时拦截 vs 放行 vs 提示重试**：08 §11 明确待确认；本期取「保守拦截」。
- `REALTIME_VOICE_RISK` 是否纳入风险优先级。
- 实时检测二次确认是否应重读 cache 看新数据。
